{
  "permissions": {
    "allow": [
      "Bash",
      "Agent",
      "Read",
      "Edit",
      "Write",
      "Glob",
      "Grep",
      "TodoWrite",
      "Task",
      "NotebookEdit",
      "WebFetch",
      "WebSearch"
    ],
    "deny": []
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "STOP. Is the answer already in the conversation? Only proceed if you genuinely need information not available in context. If the answer IS in context, output JSON: {\"decision\":\"block\",\"reason\":\"Answer is already in context — respond directly instead of launching an agent.\"}. If the information genuinely is NOT available, output: {\"decision\":\"allow\"}."
          }
        ]
      }
    ]
  }
}
